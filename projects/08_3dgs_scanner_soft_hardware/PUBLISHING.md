# Publishing to GitHub

Target repository and folder:

```text
https://github.com/lemonbaby2/-123/tree/main/projects/08_3dgs_scanner_soft_hardware
```

The project is ready locally. The preferred destination is a named folder inside the existing `lemonbaby2/-123` repository.

## Recommended Publish

If using local GitHub CLI, first clone or open `lemonbaby2/-123`, then copy this project folder to:

```text
projects/08_3dgs_scanner_soft_hardware/
```

Then commit:

```powershell
git add projects/08_3dgs_scanner_soft_hardware
git commit -m "add 3dgs scanner soft hardware project"
git push
```

## Disclosure Note

The target repository is public. The project contains PCB exports, BMS wiring notes and product engineering reports. Confirm disclosure suitability before using it for public promotion.
