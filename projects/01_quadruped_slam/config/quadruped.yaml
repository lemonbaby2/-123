frames:
  map: map
  odom: odom
  base: base_link
timing:
  imu_max_dt_s: 0.1
  lidar_expected_hz: 10
point_cloud:
  voxel_leaf_m: 0.10
  icp_max_correspondence_m: 2.0
loop_closure:
  descriptor_threshold: 0.15
  minimum_keyframe_gap: 30
